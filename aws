Instance ID

i-049660697705e057c (Sumanth)
Open an SSH client.

Locate your private key file. The key used to launch this instance is pem_key.pem

Run this command, if necessary, to ensure your key is not publicly viewable.

chmod 400 "pem_key.pem"

Connect to your instance using its Public DNS:

ec2-13-48-46-64.eu-north-1.compute.amazonaws.com

Example:


ssh -i "pem_key.pem" ec2-user@ec2-13-48-46-64.eu-north-1.compute.amazonaws.com
Note: In most cases, the guessed username is correct. However, read your AMI usage instructions to check if the AMI owner has changed the default AMI username.

